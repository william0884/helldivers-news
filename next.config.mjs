/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['giphy.com', 'media0.giphy.com', 'media3.giphy.com', 'media4.giphy.com', 'media1.giphy.com', 'media2.giphy.com'],
	},
};

export default nextConfig;
